// A re-export, so a Paper-wide change lands in one file (instruction_mds/structure.md §6). AppText is Paper's own
// Text typed for the `display` and `amount` variants (src/lib/theme.ts).
export { Text } from 'react-native-paper';
export { AppText } from '../lib/theme';
