import { PlaceholderScreen } from '../../../../components/placeholder-screen';

// A stub. Audit has no screen or mockup yet, but its rail item needs somewhere to land.
export default function Audit() {
  return <PlaceholderScreen />;
}
