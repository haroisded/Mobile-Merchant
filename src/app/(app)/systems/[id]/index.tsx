// Merchant-Home-Screen: the first screen inside a system, where a SystemCard tap lands. Blank for now
// (System-Context/Merchant-Home-Screen/additional-context.txt). The shell's `sceneStyle` paints the
// background, so rendering nothing is a blank screen rather than a transparent one.
export default function SystemHome() {
  return null;
}
