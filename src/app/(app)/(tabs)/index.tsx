import { HomeScreen } from '../../../screens/home';

// The systems list. A route file only reads its params and renders a screen (instruction_mds/structure.md rule 4).
export default function Home() {
  return <HomeScreen />;
}
